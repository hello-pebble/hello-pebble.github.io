---
layout: default
title: "사내 교육 플랫폼(LMS) 개발"
permalink: /portfolio/learning-management-system/
---

# 사내 교육 플랫폼(LMS) 개발

<span class="badge outline">회사 프로젝트</span>

> RBAC 기반의 세밀한 권한 검증과 Oracle BLOB 파일 저장 설계로, 기업용 플랫폼의 두 가지 생명선인 **보안 무결성과 데이터 정합성**을 확보한 프로젝트입니다.

---

## Project Overview

- **구분**: 회사 프로젝트
- **기간**: 2020.12 ~ 2021.03 (4개월)
- **기술 스택**: Java, Spring Boot, JPA, Oracle
- **핵심 역할**: SSO 권한 인가 로직 설계, Oracle BLOB 파일 저장소 통합 구현

---

## Intent (설계 의도)

다수 부서와 직군이 함께 쓰는 기업용 시스템에서 보안 사고와 데이터 유실은 발생 후 수습이 불가능에 가깝습니다. 그래서 기능 구현보다 먼저, **"비인가 접근이 구조적으로 불가능한 권한 체계"와 "백업 시점이 어긋날 수 없는 저장 구조"**를 설계의 최우선 순위로 두었습니다.

특히 파일 저장은 관행적인 로컬 디스크 방식 대신, 파일과 비즈니스 데이터가 항상 같은 트랜잭션 안에서 살고 죽는 구조를 선택하여 정합성 문제를 원천에서 제거하고자 했습니다.

---

## Problem (문제 정의 및 원인 분석)

- **문제**: 기업용 통합 플랫폼 특성상 민감한 사내 정보 노출과 비인가 유저의 기능 접근 위협이 있었고, 데이터베이스 백업 시 파일 누락으로 인한 정합성 불일치 리스크가 존재했습니다.
- **원인**: 여러 부서·직군이 혼재된 다수 유저를 제어할 세분화된 권한 체계(SSO 연동 권한 인가)가 부재했습니다. 또한 첨부파일을 웹 서버 로컬 디스크에 직접 보관하는 전통적인 방식은 **DB 백업과 파일 스토리지 백업 간의 시점 불일치**를 일으켜, 데이터 유실 시 복구가 매우 까다로웠습니다.

---

## Solution (해결 과정)

1. **SSO 연동 및 RBAC(Role-Based Access Control) 설계**
   - 사내 싱글사인온(SSO) 시스템과 연동한 사용자 세션 정보를 수집했습니다.
   - 역할 기반 권한 모델(RBAC)을 구조화하여, Spring Security 계층과 API 엔드포인트 도메인 레벨에서 비인가 요청을 원천 차단하는 **공통 권한 검증 인터셉터**를 구축했습니다.
2. **Oracle BLOB 기반 파일 저장 방식 채택**
   - 파일 유실 가능성을 차단하고 백업을 단순화하기 위해, 디렉토리 경로 저장 대신 DB 테이블에 이진 데이터를 직접 담는 **Oracle BLOB(Binary Large Object)** 방식을 설계했습니다.
   - 파일 생성/수정/삭제가 비즈니스 데이터 처리와 **단일 DB 트랜잭션(`@Transactional`)으로 묶여 영속**되도록 보장했습니다.

---

## Results (최종 성과)

- **보안 무결성 보장**: 정밀한 역할 권한 제어를 통해 오픈 이후 **보안 유출 사고 0건**을 유지하며 사내 배포를 완료했습니다.
- **데이터 유실 차단**: 파일 데이터를 RDBMS 테이블에 완전 통합하여 백업·복구 정합성 불일치로 인한 **데이터 유실 리스크를 근본적으로 차단**했습니다.
- **기술 내재화**: JPA를 실무에 접목하며 객체-관계 매핑(ORM) 기반의 현대적인 백엔드 설계 역량을 내재화했습니다.
