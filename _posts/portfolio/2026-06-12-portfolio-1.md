---
layout: default
title: "DelayNoMore - AI 계획 실행 서비스"
permalink: /portfolio/1/
category: portfolio
tags: [SpringBoot, Java, React, PostgreSQL, OpenRouter, Portfolio]
---

<span class="project-context">개인 프로젝트 · 기획·개발·배포</span>

# DelayNoMore — AI 계획 실행 서비스

- AI와 대화해 하루 단위 계획을 만들고, 확정 후에는 완료 체크와 이월만 허용하는 실행 중심 서비스
- 외부 LLM·게스트 접근·1GB 무료 VM의 제약에서 상태 무결성, 영속화, 장애 폴백과 배포까지 단독 구현

**[▶ 실행해 보기](http://delaynomoreapp.duckdns.org/)** · **[GitHub 저장소](https://github.com/hello-pebble/DelayNoMore_Release)**

<nav class="project-page-nav" aria-label="DelayNoMore 프로젝트 목차">
  <a href="#overview">
    <span>Overview</span>
    <small>제품 흐름과 핵심 결과</small>
  </a>
  <a href="#ai-streaming">
    <span>01. AI 응답 경험</span>
    <small>SSE·patch·비용 최적화</small>
  </a>
  <a href="#state-integrity">
    <span>02. 상태 무결성</span>
    <small>Immutable Lock·PostgreSQL</small>
  </a>
  <a href="#deployment">
    <span>03. 제약 안의 배포</span>
    <small>단일 컨테이너·Pull 배포</small>
  </a>
</nav>

## Overview {#overview}

계획을 세우는 것보다 계획을 계속 수정하는 과정이 실행을 늦춘다는 가설에서 시작했습니다. 사용자는 AI와 계획을 만든 뒤 확정하고, 확정된 계획에서는 완료 체크와 미완료 항목의 다음 날 이월만 수행합니다.

<div class="shot-labels"><span>① 계획 생성</span><span>② 오늘 할 일</span><span>③ 실행 추적</span></div>

![DelayNoMore 실제 서비스 화면 — 왼쪽 AI 코치와의 대화로 계획을 만들고, 가운데에 오늘 할 일이 모이며, 오른쪽 체크리스트에서 실행을 추적한다](/assets/images/portfolio/delaynomore-app.png){:.portfolio-hero-shot}

### 핵심 결과

| 판단 | 구현 및 검증 |
| :--- | :--- |
| **계획 확정은 서버 불변식** | 구조·내용 변경을 `409 PLAN_LOCKED`로 거부하고 트랜잭션과 행 잠금에서 검사 |
| **재시작 후에도 상태 복원** | PostgreSQL로 이전하고 Testcontainers에서 새 인스턴스의 데이터 재조회 검증 |
| **AI 대기를 진행 경험으로 전환** | SSE와 날짜별 순차 생성으로 진행 상태를 노출하고 patch 교환으로 전송량 축소 |
| **회고를 다음 계획에 반영** | 완료율·체감 난이도를 결정적 규칙으로 계산하고 AI는 추천 이유만 설명 |

### 실행 기록이 다음 계획으로 이어지는 흐름

일일 회고에서 완료율과 체감 난이도를 저장하면 서버가 다음 계획의 하루 분량을 추천합니다. 추천 숫자는 동일 입력에서 재현돼야 하므로 서버 규칙이 계산하고, AI에는 결과를 변경할 권한 없이 사용자가 이해할 수 있는 이유 설명만 맡겼습니다.

![AI 분량 추천 피드백 루프](/assets/images/portfolio/delaynomore-recommendation.svg){:.portfolio-diagram}

![오늘 마무리 화면 — 완료율과 체감 난이도, 이유를 기록한다](/assets/images/portfolio/delaynomore-reflect.png){:.portfolio-detail-shot}

---

## 01. AI 응답 대기를 진행 경험으로 바꾸기 {#ai-streaming}

### 문제

고품질 계획 생성에 20~30초가 걸려 사용자가 처리 여부를 알 수 없는 무응답 구간이 생겼습니다. 대화마다 계획 전체를 다시 주고받아 토큰도 불필요하게 사용했습니다.

### 설계

- 응답을 `산문 → 구분자 → 계획 JSON`으로 분리하고 사람이 읽는 텍스트만 SSE로 전송했습니다.
- 계획은 전체 문서 대신 변경된 날짜만 담은 patch로 교환했습니다.
- `id`·`completed` 보일러플레이트를 제거하고 대화 이력을 12턴에서 6턴으로 줄였습니다.
- 추론 모드를 끄고 `max_tokens` 상한을 적용했습니다.
- 스트림 실패 시 비스트리밍, 다시 실패하면 mock으로 전환했습니다.

![AI 응답 최적화 — patch와 SSE로 전송량과 체감 대기를 줄임](/assets/images/portfolio/delaynomore-ai.svg){:.portfolio-diagram}

### 결과와 Trade-off

- 요청당 입력은 364–1,645 토큰, 비용은 $0.0002–$0.0022 범위로 확인했습니다.
- 외부 LLM이 실패해도 계획 생성의 핵심 흐름을 확인할 수 있습니다.
- SSE는 생성 시간 자체를 줄인 것이 아니라 사용자가 진행을 알 수 없는 구간을 제거한 선택입니다.

---

## 02. 계획 상태의 무결성과 영속화 {#state-integrity}

### UI 규칙을 서버 불변식으로 옮겼다

“확정된 계획은 수정할 수 없다”는 규칙을 화면에서만 막으면 API 직접 호출로 우회할 수 있습니다.

- 확정된 계획의 구조·내용 변경은 서버가 `409 PLAN_LOCKED`로 거부합니다.
- 완료 토글만 허용하고, 거부된 요청은 저장소와 감사 이력을 변경하지 않습니다.
- 조회·검사·갱신을 트랜잭션과 `SELECT … FOR UPDATE` 안에서 처리합니다.
- 모든 변경은 세션·시각·내용을 감사 이력으로 기록합니다.

![Immutable Lock — 확정 계획을 서버와 트랜잭션에서 잠금](/assets/images/portfolio/delaynomore-lock.svg){:.portfolio-diagram}

행 잠금은 일관성을 얻는 대신 동시 처리량을 제한합니다. 잠금 범위를 사용자 계획 한 건으로 제한해 영향 범위를 좁혔습니다.

### 저장소를 교체해도 서비스 규칙은 유지했다

인메모리 저장은 서버 재시작마다 데이터가 사라져 회고를 누적할 수 없었습니다. Repository 인터페이스를 유지한 채 PostgreSQL 구현체를 추가해 서비스 코드가 저장 방식에 의존하지 않도록 했습니다.

- 형태가 변하는 계획 항목은 `JSONB`로 저장했습니다.
- 회고는 계획 삭제 시 함께 정리되도록 `ON DELETE CASCADE`를 사용했습니다.
- 감사 이력은 계획 삭제 후에도 남기기 위해 FK를 두지 않았습니다.
- 인메모리의 원자 가드와 1일 1회고 업서트를 트랜잭션과 행 잠금으로 이식했습니다.
- Testcontainers의 PostgreSQL 17에서 원자성·업서트·캐스케이드·재시작 복원을 검증했습니다.

![데이터 스키마 — 목적에 따라 선택한 저장 전략](/assets/images/portfolio/delaynomore-schema.svg){:.portfolio-diagram}

---

## 03. 1GB 무료 VM 제약 안에서 배포하기 {#deployment}

프론트엔드·백엔드·AI 프록시를 하나의 Docker 컨테이너로 묶었습니다. Spring Boot가 React 정적 파일과 `/api/v1` REST·SSE를 함께 제공하고 OpenRouter 키는 서버에만 보관합니다.

![시스템 아키텍처 — 단일 컨테이너 안의 Controller·Service·Repository](/assets/images/portfolio/delaynomore-architecture.svg){:.portfolio-diagram}

GitHub Actions가 이미지를 빌드해 GHCR에 올리고, Oracle Cloud 무료 VM은 빌드하지 않고 이미지를 Pull해 실행합니다. 저사양 VM에서 빌드가 자원을 점유하는 문제를 피하고 JVM 힙 상한으로 OOM을 방어했습니다.

![배포 파이프라인 — VM은 빌드하지 않고 이미지를 Pull한다](/assets/images/portfolio/delaynomore-deploy.svg){:.portfolio-diagram}

단일 컨테이너는 구성 요소별 독립 확장에 불리합니다. 하지만 현재 트래픽과 1GB VM에서는 확장성보다 배포 단순성과 장애 지점 축소를 우선했습니다.

### 의도적으로 남긴 한계

- **게스트 기반 접근**: 브라우저의 게스트 ID가 데이터 접근 키이므로 다른 브라우저에서 기존 데이터에 다시 연결할 수 없습니다.
- **단일 서버 전제**: 생성 한도 가드에 `synchronized`를 사용해 다중 서버에서는 한도가 최대 1건 초과될 수 있습니다.
- **문자열 기반 목표 계보**: 같은 목표를 `goalName`으로 묶어 이름을 바꾸면 추천 이력이 나뉩니다.

추상적인 다중 서버 확장보다 현재 운영 범위에서 보장할 규칙을 먼저 명확히 했습니다. 로그인, 분산 동시성, 구조적 목표 계보는 다음 마일스톤으로 분리했습니다.

## 더 보기

- [CHANGELOG](https://github.com/hello-pebble/DelayNoMore_Release/blob/main/CHANGELOG.md) — 13개 릴리스의 변경 이력과 설계 결정
- [DelayNoMore Release](https://github.com/hello-pebble/DelayNoMore_Release) — 소스 코드·API·배포 스크립트
- [원본 프로토타입](https://github.com/hello-pebble/DelayNoMore)
