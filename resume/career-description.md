# 경력기술서

**권다경** · Backend Developer · Java / Spring
kwdk2323@gmail.com · github.com/hello-pebble

**Java/Spring 기반 백엔드 개발자 (경력 3년 10개월)**
운영과 유지보수 중심의 프로젝트에서 REST API 개발, Docker 기반 배포, GitLab CI 구축, 장애 원인 분석과 운영 개선을 담당했습니다. AI 학습데이터 라벨링 공정 관리, 공공 개방데이터 API, 정부 문서 처리 시스템을 거치며 **배포 실패·장애 대응·사용자 문의·온보딩 같은 반복되는 운영 비용을 줄이는 설계와 절차화**에 강점이 있습니다.

| 기간 | 회사 | 담당 프로젝트 |
| :--- | :--- | :--- |
| 2022.11 ~ 2025.01 | (주)미디어그룹사람과숲 | 공정 관리 솔루션(PMS) 개발·운영 / 개방DB API관리 시스템 / 정부 오픈API 개방 관리 |
| 2020.12 ~ 2021.03 | (주)에이아이넷 | 사내 교육 플랫폼(LMS) 백엔드 |
| 2019.05 ~ 2020.07 | (주)엘에프아이티 | 항만 시설 사용료 관리 시스템 |

**주요 프로젝트**

| 프로젝트 | 증명하는 것 | 대표 성과 |
| :--- | :--- | :--- |
| **1. PMS 개발·운영** | 운영 · 장애 분석 · 배포 · 데이터 정합성 · 문서화 | 사용 문의 **월 20건 → 1~2건 (90%↓)** |
| **2. 개방DB API관리** | API 설계 · 메타데이터 · 보안 · 레거시 개선 · 테스트 | 조회 API 추가 **파일 5~6개 → 등록 1건** |
| **3. DelayNoMore** (개인) | 트랜잭션 · 동시성 · SSE · AI · 테스트 · 배포 | 에이전트 도구 선택 **676회 실측**, 스모크 **57회 100%** |

| 구분 | 내용 |
| :--- | :--- |
| 언어·프레임워크 | Java 8/21, Kotlin, Python · Spring Boot, Spring MVC, Spring Security, eGovFrame |
| 데이터 | MariaDB, Oracle, Tibero, PostgreSQL · MyBatis, JPA, jOOQ, Flyway |
| 인프라·배포 | Docker, Docker Compose, GitLab CI, GitHub Actions, WildFly, Tomcat, Nginx |
| 테스트 | JUnit 5, MockMvc, Testcontainers · 단위/통합 분리, 회귀 테스트 |
| 비동기·실시간 | SSE(`SseEmitter`), Long Polling(`DeferredResult`), WebSocket |

---

# 주요 프로젝트 1. 사내 공정 관리 솔루션(PMS) 개발 및 운영

| | |
| :--- | :--- |
| **역할** | 백엔드 개발 · 운영 (툴 통합·커스터마이징, 배포 환경 구성, 장애 대응, 사용자 교육) |
| **기간·규모** | 2022.11 ~ 2024.12 (2년 2개월) · 사용자 최대 100명(관리자·매니저·기간별 작업자) |
| **핵심 성과** | 사용 문의 **월 20건 → 월 1~2건 (약 90% 감소)** · 설정 누락으로 인한 전면 접속 장애를 배포 전 단계에서 차단 · 작업분 유실 장애의 **3개 원인 규명 및 조치** |
| **기술** | Java, Spring Boot, MyBatis, MariaDB, Docker / Docker Compose, GitLab CI, Nginx |

AI 학습데이터 라벨링 공정을 관리하는 웹 시스템으로, **3D(PCD 큐보이드 + 2D 좌표 투영)·2D 이미지·PDF 세 종류의 라벨링 툴을 시스템 안에 포함**한다. 도입 전에는 라벨링이 독립 실행 프로그램으로 이뤄져 매니저가 작업자 PC에 설치한 뒤 엑셀 파일명으로 작업물을 배분·회수했는데, 이를 PMS 안의 웹 툴로 옮기고 배분–라벨링–검수–납품을 하나의 공정으로 묶었다.

## 핵심 문제

| 문제 | 비즈니스 비용 |
| :--- | :--- |
| 포트·방화벽·컨테이너 연결 중 하나만 어긋나면 전원 접속 불가. 그 설정이 문서가 아니라 담당자 기억에 있었다 | 접속 불가 = **라벨링·검수·납품 일정 전체의 정지** |
| 대량 저장 시 작업분 유실 — 중복 저장, 뒷부분 저장 실패, 미저장 종료 | 라벨링 결과가 곧 납품물이라 **한 건 유실 = 재작업** |
| 프로젝트가 분기마다 새로 생기고 담당자·작업자가 바뀌어 같은 질문이 반복 | 개발자 시간이 문의 대응에 소모, **초기 월평균 20건** |

## 핵심 의사결정

**1. 실행 환경을 이미지로 고정했다.** 장애 원인 대부분이 애플리케이션 버그가 아니라 서버마다 다른 포트·경로·런타임이었다. 같은 WAR가 개발 서버에서는 뜨고 운영 서버에서는 뜨지 않는 문제는 코드로 해결할 수 없다. Docker Compose로 실행 환경을 이미지화해 서버 간 환경 차이를 제거하고, **DB는 컨테이너 밖으로 분리**해 배포 실수가 데이터 손실로 이어지지 않게 했다.

**2. 빌드는 자동, 배포는 수동 승인.** 소스가 이미 GitLab에 있어 관리 지점을 늘리지 않도록 GitLab CI로 이미지 빌드까지 자동화했다. 배포 실행은 수동으로 남겼다 — 작업자들의 작업 종료 확인 후에만 배포가 가능하고 배포 직후 동작 확인을 사람이 해야 하는 운영 조건에서, 자동 배포로 시점이 심야에 잡히면 그 확인을 할 사람이 없기 때문이다. 대신 배포 전 확인 항목(포트 포워딩·방화벽·컨테이너 포트 연결)을 체크리스트로 고정해 누가 해도 같은 결과가 나오게 했다.

**3. 권한을 결과물 보호 기준으로 나눴다.** 라벨링 결과가 그대로 납품 데이터가 되므로, 작업자–검수 경로 밖에서 값이 바뀌면 납품물의 추적성을 보증할 수 없다. 매니저는 배분·현황 조회·문의 답변까지 가능하되 **라벨링 결과는 열람만**, 수정·삭제는 관리자로 한정했다.

## 대표 구현 — 저장 실패 원인 분석과 조치

가장 많은 문의가 나온 저장 장애는 **하나의 증상 뒤에 원인이 셋**이었다.

| # | 원인 |
| :--- | :--- |
| 1 | 자동 라벨링(한 프레임 지정 → 폴더 전체 추적 생성)으로 불어난 결과가, **로드된 전체 프레임의 모든 객체를 요청 하나로 전송**하는 오픈소스 저장 구조와 맞물려 요청이 서버 저장 처리에 도달하기 전에 끊김 |
| 2 | 객체 단위 INSERT를 **요청 전체 트랜잭션 없이 건별 커밋** — 중간에 끊기면 앞부분만 남음. "앞은 저장, 뒤는 유실"은 부분 저장이 아니라 **부분 커밋**이었다 |
| 3 | 3D 저장의 유니크 키에 객체 클래스·순서 번호까지 묶여 값이 바뀌면 신규 행이 되는데, 2D 경로에 있던 **기존 행 정리 호출이 3D 경로에는 누락**돼 이전 행이 잔존 |

조치는 **저장 단위를 프레임 단위로 분할**해 요청 하나의 크기가 폴더 전체에 비례하지 않게 한 것이다. 요청 전체를 한 트랜잭션으로 묶는 근본 조치도 검토했으나, 운영 중인 툴이라 저장 경로 전체를 건드리면 회귀 범위가 라벨링 작업 전체가 된다 — 매일 발생하는 유실을 먼저 멈추고 구조 변경을 뒤로 미뤘다. 함께 파일 이동 시점 자동 저장과 창 닫기 시 확인 안내를 넣어 미저장 종료 경로를 막았고, 조치 후 대량 자동 라벨링 상태에서 저장 성공과 중복 미발생을 확인했다.

**그 외 구현** — 신규 프로젝트마다 요구되는 라벨링 툴 기능 개발과 프로젝트별 커스터마이징 / 작업·검수 모드 분리(최종 승인 건만 규격 JSON 다운로드가 열려 미검수 데이터가 납품 경로로 나갈 수 없음) / 엑셀 기반 대량 사용자 등록 / 출퇴근·유급무급 휴가 승인 간이 HR / 동작·에러·접근 로그 분류 수집과 에러 패턴 기반 장애 대응 매뉴얼

## 결과

| 항목 | 이전 | 이후 |
| :--- | :--- | :--- |
| **사용 문의** | **월평균 20건** (초기) | **월 1~2건** (1년 후, **약 90% 감소**) |
| 작업물 배분 | 로컬 설치 프로그램 + 엑셀, 담당자 부재 시 작업 중단 | 웹 원격 배분·회수, 담당자 부재와 무관 |
| 배포 | 담당자 기억 의존, 설정 하나 누락으로 전원 접속 불가 | 이미지로 환경 고정 + 체크리스트, 배포 전 차단 |
| 저장 실패 | 대량 저장 시 요청 유실·부분 커밋·중복 | 3개 원인 규명 후 프레임 단위 분할로 실패 조건 제거 |
| 신규 담당자 인계 | 구두 설명 의존 | PDF 매뉴얼 + 화면 내 매뉴얼 버튼 + 분기별 시연 |

문의 90% 감소는 문서를 한 번 쓴 결과가 아니다. 화면별 기능·조작을 번호로 설명한 PDF 매뉴얼을 만들고 기능이 몰린 화면에는 **서비스 안에 매뉴얼 버튼**을 두었으며, 문의를 받으면 그 항목을 매뉴얼에 추가했다. 분기별 신규 담당자 시연과 전 사원 대상 워크숍 공유, 프로젝트 시작마다 팀별 서버 세팅과 인계를 맡았다.

## 회고

**부분 실패는 전면 실패보다 늦게 발견된다.** 저장 장애는 실패를 드물게 만들었을 뿐 트랜잭션 경계는 끝내 도입하지 못했다. 다시 한다면 실패 시 전부 되돌아가는 경계를 먼저 만들고 그 위에서 전송량을 줄였을 것이다 — 데이터가 절반만 맞는 상태는 발견이 늦고, 발견됐을 때는 이미 납품 데이터에 섞여 있다.

**측정한 것만 개선을 말할 수 있다.** 문의 건수는 세고 있었기에 90% 감소를 말할 수 있지만 배포 소요 시간과 장애 대응 시간은 세지 않았다. 지금이라면 배포 시작–확인 완료, 장애 인지–복구 시각을 기록하는 것부터 했을 것이다. 장애 매뉴얼도 갱신 책임자와 주기를 정하지 않으면 빠르게 낡는다는 것을 배웠다.

---

# 주요 프로젝트 2. 개방DB API관리 시스템 설계 및 프로토타입 개발

| | |
| :--- | :--- |
| **역할** | 화면·API 설계 · 메타데이터 구조 설계 · 동적 쿼리 엔진 구현 · 권한 분리 · 관리자 대시보드 (3인 팀) |
| **기간** | 2024.09 ~ 2025.01 (5개월) |
| **핵심 성과** | 조회 API 추가 공수 **파일 5~6개 + 재배포 → 메타데이터 등록 1건** · 검증 누락 가능 지점 **API 개수만큼 → 엔진 1곳** · 재구현 검증 **테스트 50건 CI** |
| **기술** | Java, Spring, MyBatis, JSP, JavaScript, MariaDB, Chart.js |

오랫동안 사용해온 레거시가 구조화돼 있지 않아 **신규 투입 인력이 어떤 모듈을 써야 하는지 파악하는 데 시간이 걸렸고 프로젝트 초반 딜레이가 반복**됐다. 3인 팀으로 신규 프로젝트를 진행하면서 동시에 재사용 가능한 관리 시스템 베이스를 만드는 것이 목표였다.

## 핵심 문제

| 문제 | 비즈니스 비용 |
| :--- | :--- |
| 신규 조회 요구사항마다 Controller·Service·ServiceImpl·DAO·SQL XML **5개 파일**(관리 화면 동반 시 6개) 추가. 파라미터는 VO 없이 Map으로 오가 컴파일 검증도 안 됨 | 요구사항 1건 = 개발·리뷰·배포 1사이클 |
| 응답 항목 변경 시 기존 API 수정(사용자 파손)과 신규 생성(유사 API 누적) 중 기준이 없음 | 시간이 지나면 **어떤 API가 유효한지 아무도 모름** |
| 파라미터 검증·바인드 처리를 빠뜨릴 수 있는 자리가 API마다 존재 | 외부 공개 API에서 누락 하나가 **SQL 인젝션 통로** |

## 핵심 의사결정

**1. 반복을 코드가 아니라 데이터로 옮겼다.** 조회 기능의 공통 부분을 메타데이터와 SQL 변환 엔진으로 모으고 업무별 차이는 설정으로 표현했다. 코드 제너레이터도 검토했지만 파일 수가 그대로 늘어난다. 이 선택은 온보딩 문제와 뿌리가 같다 — 신규 인력이 헤맨 이유는 코드가 많아서가 아니라 **어디를 봐야 하는지 알 수 없어서**였고, 엔진 하나로 모으면 학습 대상이 "엔진 1개 + 메타데이터 규칙"으로 고정된다.

**2. 동적 영역의 경계를 먼저 그었다.** 런타임 조립 SQL은 컬럼·조건 조합마다 DB 방언 차이가 드러나고, 운영 DB는 개발 단계에서 접근할 수 없었다. **동적 영역을 넓힐수록 검증 경로가 `DB 종류 × 조건 조합`만큼 곱으로 늘어나므로** 층을 나눠 처리했다.

| 층 | 대상 | 처리 |
| :--- | :--- | :--- |
| 바깥 | 페이징 | 공통 래퍼를 XML에 고정, 값은 바인드 변수 |
| 중간 | DB 방언 | SQL 맵 세트를 디렉터리 단위로 분리해 설정으로 교체 |
| 안쪽 | 조회 컬럼·조건 | 메타데이터 기반 동적 조립 |

**3. 권한은 되돌릴 수 있는 동작인지로 나눴다.** 신규 데이터 등록과 API 버전 생성은 일반 사용자에게 열었다 — 잘못돼도 내부에서 수정 가능하고, 여기를 막으면 병목이 생겨 시스템을 안 쓰게 된다. 반면 **데이터 개방 여부 설정은 팀장 권한으로 한정**했다. 외부 공개는 되돌릴 수 없고 사업·법적 검토가 필요하기 때문이다. 함께 동일 데이터셋에 **API 버전(v1, v2) 체계**를 도입해, 기존 사용자를 깨지 않으면서 새 요구를 수용하고 두 버전의 관계가 시스템에 기록되게 했다.

## 대표 구현 — 메타데이터 기반 조회 엔진

API 엔드포인트 하나가 메타데이터 테이블의 행 하나다. 데이터셋이 조회 대상 테이블과 URL 키를, 컬럼 정의가 노출 컬럼·허용 필터·정렬 여부를 담고 **여기 등록된 컬럼만** 쓸 수 있다.

| sourceColumn | filterType | sortable | 요청 예시 |
| :--- | :--- | :--- | :--- |
| BILL_ID | EQUALS (=) | ✓ | `?BILL_ID=2200001` |
| COMMITTEE | CHECK (IN) | ✓ | `?COMMITTEE=행정안전위원회,정무위원회` |
| PROPOSE_DT | DATE (BETWEEN) | ✓ | `?PROPOSE_DT=2026-01-01,2026-06-30` |

**인젝션 방어 지점은 엔진 한 곳이다.** SQL에 들어가는 식별자는 요청 파라미터명이 아니라 메타데이터에 저장된 컬럼명이고, 요청 파라미터명은 조회 키로만 쓰이고 버려진다. 연산자도 클라이언트가 아닌 컬럼의 `filterType`이 결정하며, 값은 전부 바인드 파라미터다. 블랙리스트가 아닌 화이트리스트라 방어 범위가 규칙 목록의 완전성에 의존하지 않는다.

그 외 데이터 타입 검증, 등록 직후 샘플 URL 호출로 설정 오류를 배포 전에 잡는 장치, Chart.js 기반 호출 통계·인기 데이터셋 대시보드, 레거시 모듈화와 API 명세서·설계 문서 작성, iBatis → MyBatis 전환을 수행했다.

## 검증 — 미완의 검증을 재구현으로

로컬·개발 서버에서 메타데이터 등록만으로 신규 API가 동작하는지, 미등록 컬럼·미허용 정렬이 차단되는지 확인했으나 **운영 반영 전 이직해 실트래픽 검증은 수행하지 못했다.**

그 검증을 대신하기 위해 핵심 구조를 비식별화해 현대 스택으로 재구현한 것이 **[api-forge](https://github.com/hello-pebble/api-forge)**(Java 21 · Spring Boot 3.5 · jOOQ · Testcontainers)다. **테스트 50건**이 매 커밋 CI에서 방어를 증명한다 — 미등록 컬럼 필터 400, `BILL_ID; DROP TABLE …` 파라미터명 위장 400, `' OR '1'='1` 값 주입은 문자열 리터럴 처리. 동일 시나리오를 H2·PostgreSQL 양쪽에 두어 방언 차이까지 확인한다. 레거시 경험을 기준으로 문자열 연결 SQL + 블랙리스트는 **jOOQ 타입 세이프 DSL + 식별자 화이트리스트**로, Map 파라미터는 DTO + Bean Validation으로, 평문 인증키는 해시 저장으로 대체했다.

## 결과

| 항목 | 이전 | 이후 |
| :--- | :--- | :--- |
| **신규 조회 기능 추가** | 파일 5~6개 작성 + 빌드·재배포 | **메타데이터 등록 1건**, 빌드·재배포 없음 |
| **검증 누락 가능 지점** | API 개수만큼 | **엔진 1곳** |
| 동일 데이터셋 요구 변경 | 기존 API 수정(파손) 또는 유사 API 중복 | v1·v2 공존, 후속 관계가 시스템에 기록 |
| 개방 여부 설정 | 등록 권한과 동일 | 팀장 권한 계정으로 분리 |
| 신규 인력 학습 대상 | 흩어진 조회 로직, 구조 문서 없음 | 엔진 1개 + 메타데이터 규칙 + 명세서·설계 문서 |
| 데이터셋 활용 현황 | 확인 수단 없음 | 대시보드로 호출 통계·인기 데이터셋 확인 |

## 회고

**추상화의 이득은 검증 비용과 함께 계산해야 한다.** "모든 것을 설정으로" 밀어붙였다면 공수는 더 줄었겠지만, 접근할 수 없는 운영 DB를 상대로 조합 폭발한 검증 경로를 감당할 수 없었을 것이다. 페이징을 고정 래퍼로 남긴 판단이 가장 실용적이었다.

**설정으로 옮긴 문제는 사라지지 않고 위치를 바꾼다.** 코드 오류가 메타데이터 오류가 됐을 뿐이라, 재구현에서 식별자 규칙 검증과 발행 전 소스 테이블 실존 확인을 넣었지만 발행 후 스키마 변경 감지는 과제로 남았다. 다만 회사 코드를 가져올 수 없다는 제약 때문에 검증을 포기하는 대신, 구조만 재구현해 테스트로 증명하는 방법이 있다는 것을 확인했다.

---

# 주요 프로젝트 3. DelayNoMore — AI 계획 실행 지원 서비스 (개인)

| | |
| :--- | :--- |
| **역할** | 기획 · 데이터 모델링 · API 설계 · 프론트/백엔드 구현 · 평가 하네스 · 데모 배포 (단독) |
| **기간** | 2026.04 ~ 2026.06 · v0.1.0 ~ v0.17.0, 버전마다 문제 하나를 정해 해결 |
| **핵심 성과** | JUnit 5 **317건** 3층 분리 · 에이전트 도구 선택 **676회 실측**으로 권한 우회 발견 · 릴리스 스모크 **57회 통과율 100%** 후 배포 |
| **기술** | Java, Spring Boot, React, PostgreSQL, OpenRouter, Docker, GitHub Actions, Oracle Cloud |

[github.com/hello-pebble/DelayNoMore_Release](https://github.com/hello-pebble/DelayNoMore_Release) · [데모](http://delaynomoreapp.duckdns.org/)

AI와 대화해 하루 단위 계획을 만들고, **확정 후에는 완료 체크와 이월만 허용**하는 서비스. 각 버전이 직전 버전이 드러낸 문제 하나를 해결하는 방식으로 진행했다.

## 핵심 의사결정

**1. 규칙의 소유권을 프론트에서 서버로, 다시 도구 권한으로.** "확정된 계획은 수정할 수 없다"는 규칙이 세 번 자리를 옮겼다.

| 단계 | 위치 | 결과 |
| :--- | :--- | :--- |
| v0.4.1 | 프론트 버튼 차단 | **curl로 API를 직접 호출하면 그대로 뚫림** — 화면의 규칙은 UX일 뿐 강제가 아니다 |
| v0.8.0 | 서버 가드 `409 PLAN_LOCKED` | 프론트는 안내, 강제는 서버. 이후 진행률·이월·날짜 검증까지 프론트의 규칙을 서버로 이관 |
| v0.14~15 | 선언적 전이표 → **도구 노출 권한** | AI 코치를 도구 호출 에이전트로 전환할 때 전이표가 그대로 도구 권한이 되면서, 수정 차단이 **애초에 수정 도구를 주지 않는 구조적 불가능**이 됨 |

전이는 `SELECT … FOR UPDATE` 행 잠금 안에서 원자적으로 실행되어 동시 완료 요청 시 첫 요청만 성공한다. 전이 매트릭스는 전수 검증 테스트로, 생성 한도 같은 동시 요청 경쟁은 20스레드가 `CountDownLatch` 이중 배리어로 동시에 진입하는 TOCTOU 테스트로 고정했다.

**2. 20~30초 AI 응답 — 모델을 낮추는 대신 대기를 진행 경험으로.** 응답 품질이 서비스의 핵심 가치라 모델을 낮추면 기능 자체가 의미를 잃는다. `SseEmitter` 기반 SSE 스트리밍으로 생성 과정을 그대로 보여주고, 계획 전체를 매번 재전송하던 방식을 **변경분(patch) 교환**으로 바꿔 토큰을 줄였다. 에이전트 루프와 SSE 전송 사이에는 `AgentEventSink`를 두어 루프를 SSE 없이 단위 테스트할 수 있게 분리했고, 스트림 실패 시 비스트리밍 → mock 폴백을 실제로 끊어보며 확인했다.

**3. 숫자는 서버 규칙이, 이유는 AI가.** 회고 기록으로 다음 계획 분량을 추천할 때 계산을 LLM에 맡기지 않았다 — 같은 기록에 추천이 달라지면 신뢰를 줄 수 없고, 외부 API 장애가 곧 기능 장애가 되기 때문이다. AI가 실패해도 서버 템플릿으로 초안은 나온다.

## 대표 구현 — 육안 확인을 평가 하네스로

AI 코치를 도구 호출 에이전트로 전환하자 **프롬프트나 모델을 바꿨을 때 좋아졌는지 판단할 수단이 없었다.** 기능 추가를 멈추고 실제 모델로 도구 선택 정확도를 재는 평가 하네스를 먼저 만들었다(케이스 데이터셋, 순수 함수 채점기, 정확도·토큰 비용 리포트).

**676회를 돌리는 동안 결론이 세 번 바뀌었다.** 처음에는 특정 케이스가 실패한다고 봤지만 반복하니 실패가 케이스를 옮겨 다녔고, 반복을 늘리자 기저 실패율이 예상의 절반으로 수렴했다. 가장 중요한 발견은 **모델이 금지된 도구 대신 허용된 인접 도구로 우회**하고 있었다는 것이다 — 잠긴 계획의 수정이 막히자 이월 도구로 같은 효과를 냈다. 권한 모델이 구조로 막을 수 없는 층이라 측정 없이는 발견할 수 없었다. 이 발견으로 `avoidTools` 채점 범주를 신설하고 하네스를 병렬화한 뒤, 릴리스 스모크 **57회 통과율 100%**를 확인하고 배포했다.

## 검증과 배포

JUnit 5 **317건**을 순수 단위 / standalone MockMvc / `*IT` 통합(Testcontainers PostgreSQL) 세 층으로 나누고, 실제 모델 호출 평가는 `@Tag("eval")`로 일반 빌드에서 제외했다. 매 push마다 GitHub Actions CI가 실행된다. 잠금 상태 수정 요청 409, 동시 완료 요청 시 첫 요청만 성공, 재시작 후 상태 복원, 스트리밍 폴백을 각각 시나리오로 확인했다.

배포는 프론트·백엔드·AI 프록시를 단일 Docker 컨테이너로 묶어 GitHub Actions가 빌드해 GHCR에 올리고, **Oracle Cloud 무료 VM(1GB)은 Pull만 해서 실행**한다 — 저사양 VM에서 빌드가 자원을 점유하면 서비스가 멈추기 때문이고, JVM 힙 상한으로 OOM을 방어했다.

## 회고

**규칙을 어디에 두느냐가 곧 그 규칙의 강제력이다.** 프론트 차단 → 서버 가드 → 도구 권한으로 옮겨온 과정이 가장 값진 학습이었다. 그리고 **출력이 흔들리는 대상은 한 번의 실행으로 판단하면 안 된다** — 반복 측정 없이는 우회 경로처럼 구조로 막을 수 없는 문제를 아예 발견하지 못한다.

**한계** — 데모 배포라 운영 트래픽과 장애 대응은 검증하지 못했고 실사용자 효과 지표가 없다. 생성 한도 가드가 `synchronized` 기반이라 다중 서버에서는 한도가 최대 1건 초과될 수 있다.

---

# 기타 경력

**정부 오픈API 개방 관리 시스템** · (주)미디어그룹사람과숲 · 2023.08 ~ 2024.02 · Java, Spring MVC, Python, WildFly, MariaDB, Chart.js
행정안전부 개방데이터 시범 사업. 4인 팀 중 Java 개발 인력이 제한적이어서 백엔드와 서버 세팅을 주도했다. **외부 통신이 차단된 폐쇄망**이라 기존 HWP 파싱 Python 코드를 그대로 쓸 수 없었고, 별도 서버 + HTTP 연동은 방화벽 규칙 추가 절차 자체가 비용이었다. 서버 로컬에 Python 가상환경을 구축하고 Java가 `ProcessBuilder`로 호출하는 **서브프로세스 연동**을 택해 네트워크 구성을 건드리지 않고 파일 입력 → 파싱 → DB 저장을 자동화했다. 공공데이터포털 연계 **오픈API 5종** 개발·WildFly 배포, 발주처가 직접 JSON 응답을 확인하는 미리보기 페이지, 호출 로그 기반 관리자 통계 화면을 구현해 시범 사업을 완료했다. ([공공데이터포털 공개](https://www.data.go.kr/data/15125451/openapi.do))

**사내 교육 플랫폼(LMS) 백엔드** · (주)에이아이넷 · 2020.12 ~ 2021.03 · Java, Spring Boot, JPA, Oracle, Swagger, SSO
6인 팀 파견, 프리랜서 선임 개발자와 협업해 강의 등록·수강생 관리·학습 자료 등록의 백엔드를 전담했다. 화면 단위 분기만으로는 비인가 접근을 막을 수 없어 SSO 세션 정보를 **RBAC**으로 구조화하고 Spring Security 계층의 공통 인터셉터에서 **엔드포인트 레벨로 차단**했다 — 검증 로직이 흩어지면 누락된 한 곳이 곧 구멍이 되기 때문이다. 첨부파일은 DB·파일 백업 시점이 어긋나 복구 시 정합성이 깨지는 문제를 피하기 위해 **Oracle BLOB**으로 저장해 파일 변경이 비즈니스 데이터와 단일 트랜잭션으로 묶이게 했다. 처음 접한 JPA를 선임의 코드 리뷰와 페어 프로그래밍으로 학습하며 실무에 적용했다.

**항만 시설 사용료 관리 시스템** · (주)엘에프아이티 · 2019.05 ~ 2020.07 · Java, Spring MVC, MyBatis, Oracle
여수광양항만공사의 GIS 기반 시스템에서 연도별·임시 세율 관리와 업체 관리 기능을 개발하고 사용료 산정을 담당했다. **월 1회 현장을 방문**해 서버를 점검하고 담당자 문의·개선 요청을 반영했다. 세율이 매년 바뀌는 도메인이라 바뀌는 값은 데이터로, 바뀌지 않는 규칙은 코드로 분리해야 한다는 것을 처음 배웠다.

---

# 기타 개인 프로젝트

**MatchSimulation — 교체 가능한 경계를 먼저 설계한 매칭 서비스 백엔드** · 2026.06 ~ 진행 중 · Java 21, Spring Boot 4.1, JPA, Flyway, Spring Security/JWT, WebSocket
"나중에 반드시 바뀔 부분"의 경계를 먼저 긋고 **실제로 교체해** 설계를 검증하는 프로젝트. 더미 토큰 → Spring Security + JWT, `ddl-auto` → Flyway로 교체 후 **회귀 테스트 51건** 통과. 채팅 수신을 Short Polling → Long Polling(`DeferredResult`) → WebSocket으로 진화시키며 각 단계 비용을 실측했다(빈 응답 **90% → 0**, push 지연 **9ms**). 동시 수락 경쟁은 `@Version` 낙관적 락으로 1건만 성공. phase별 계획서·완료 보고서 29건 공개.
[github.com/hello-pebble/MatchSimulation](https://github.com/hello-pebble/MatchSimulation)

**Gateway 기반 인증·인가 검증** · 2026.01 ~ 2026.03 · Kotlin, Spring Boot, Spring Security, OAuth2/JWT, Spring Cloud Gateway, Docker Compose
Auth가 JWT를 중앙 발급하고 Gateway·Resource Server가 JWKS로 각자 검증하는 구조를 **6개 모듈**로 구성한 아키텍처 실험. 공개 경로 접근·무토큰 401·양측 권한 검증·내부 경로 404 **4개 시나리오를 실제 요청으로 검증**했고, Ordered Locking(항상 작은 ID부터 잠금)으로 데드락을 구조적으로 배제해 **10스레드 동시 상호 선택에서 매칭 정확히 1건** 생성을 확인했다. 접근법 비교와 버그 리포트를 포함한 문서 56건을 코드와 함께 버전 관리.
[github.com/hello-pebble/oauth2-authorization](https://github.com/hello-pebble/oauth2-authorization)

---

# 학력 · 자격

- 국가평생교육진흥원 · 정보통신공학
- 동양미래대학교 · 정보통신공학
- 네트워크관리사 2급 / 리눅스마스터 2급
